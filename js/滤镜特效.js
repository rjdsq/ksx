(function() {
    var s = document.createElement('style');
    s.textContent = ".img-wrap, #v img, #lv img { filter: contrast(1.2) saturate(1.1); transition: filter 0.5s ease; }";
    document.head.appendChild(s);
})();
